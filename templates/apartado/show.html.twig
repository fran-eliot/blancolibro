{% extends 'base.html.twig' %}

{% block title %}Apartado{% endblock %}

{% block body %}
    <h1>Apartado</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ apartado.id }}</td>
            </tr>
            <tr>
                <th>TituloApartado</th>
                <td>{{ apartado.tituloApartado }}</td>
            </tr>
            <tr>
                <th>ContenidoApartado</th>
                <td>{{ apartado.contenidoApartado }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_apartado_index') }}">back to list</a>

    <a href="{{ path('app_apartado_edit', {'id': apartado.id}) }}">edit</a>

    {{ include('apartado/_delete_form.html.twig') }}
{% endblock %}
