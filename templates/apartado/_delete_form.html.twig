<form method="post" action="{{ path('app_apartado_delete', {'id': apartado.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ apartado.id) }}">
    <button class="btn">Delete</button>
</form>
