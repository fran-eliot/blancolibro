{% extends 'base.html.twig' %}

{% block title %}Contacto{% endblock %}

{% block body %}
    <h1>Contacto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ contacto.id }}</td>
            </tr>
            <tr>
                <th>NombreContacto</th>
                <td>{{ contacto.nombreContacto }}</td>
            </tr>
            <tr>
                <th>ConsultaContacto</th>
                <td>{{ contacto.consultaContacto }}</td>
            </tr>
            <tr>
                <th>EmailContacto</th>
                <td>{{ contacto.emailContacto }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_contacto_index') }}">back to list</a>

    <a href="{{ path('app_contacto_edit', {'id': contacto.id}) }}">edit</a>

    {{ include('contacto/_delete_form.html.twig') }}
{% endblock %}
