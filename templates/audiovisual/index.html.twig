{% extends 'base.html.twig' %}

{% block title %}Audiovisual index{% endblock %}

{% block body %}
    <h1>Audiovisual index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>TipoPadre</th>
                <th>FkSeccion</th>
                <th>FkApartado</th>
                <th>FkSubapartado</th> 
                <th>RutaAudiovisual</th>
                <th>PieAudiovisual</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for audiovisual in audiovisuals %}
            <tr>
                <td>{{ audiovisual.id }}</td>
                <td>{{ audiovisual.tipoPadre }}</td>
                <td>{{ audiovisual.fkSeccion }}</td>
                <td>{{ audiovisual.fkApartado }}</td>
                <td>{{ audiovisual.fkSubapartado }}</td>
                <td>{{ audiovisual.rutaAudiovisual }}</td>
                <td>{{ audiovisual.pieAudiovisual }}</td>
                <td>
                    <a class="btn btn-primary" href="{{ path('app_audiovisual_show', {'id': audiovisual.id}) }}">show</a>
                    <a class="btn btn-primary" href="{{ path('app_audiovisual_edit', {'id': audiovisual.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_audiovisual_new') }}">Create new</a>
{% endblock %}
