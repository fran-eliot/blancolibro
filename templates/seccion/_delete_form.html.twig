<form method="post" action="{{ path('app_seccion_delete', {'id': seccion.id}) }}" onsubmit="return confirm('¿Estás seguro de que quieres eliminar este elemento?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ seccion.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
