{% extends 'base.html.twig' %}

{% block title %}Subapartado{% endblock %}

{% block body %}
    <h1>Subapartado</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subapartado.id }}</td>
            </tr>
            <tr>
                <th>FkApartado</th>
                <td>{{ subapartado.fkApartado }}</td>
            </tr>
            <tr>
                <th>TituloSubapartado</th>
                <td>{{ subapartado.tituloSubapartado }}</td>
            </tr>
            <tr>
                <th>ContenidoSubapartado</th>
                <td>{{ subapartado.contenidoSubapartado }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_subapartado_index') }}">back to list</a>

    <a class="btn btn-primary" href="{{ path('app_subapartado_edit', {'id': subapartado.id}) }}">edit</a>

    {{ include('subapartado/_delete_form.html.twig') }}
{% endblock %}
