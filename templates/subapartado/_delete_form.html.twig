<form method="post" action="{{ path('app_subapartado_delete', {'id': subapartado.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subapartado.id) }}">
    <button class="btn">Delete</button>
</form>
